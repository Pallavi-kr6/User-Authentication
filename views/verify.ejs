<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Verify OTP - Halloween</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Creepster&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Creepster', cursive;
    }
    .ghost-shadow {
     text-shadow: 0 0 5px #71342e, 0 0 10px #7f1a11, 0 0 20px #83403a;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-black via-gray-800 to-purple-900 min-h-screen flex items-center justify-center">
  <div class="bg-black/70 backdrop-blur-md p-10 rounded-3xl shadow-2xl border-2 border-orange-600 w-full max-w-md">
    <h2 class="text-4xl font-bold text-orange-500 mb-6 text-center ghost-shadow">Verify Email 🎃</h2>

    <form action="/verify-otp" method="POST" class="space-y-5">
      <div>
        <label for="email" class="block text-orange-400 font-medium mb-2">Email address</label>
        <input type="email" id="email" name="email" required
          class="w-full px-4 py-2 border border-orange-600 bg-black/50 text-orange-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-transparent placeholder-orange-500">
      </div>

      <div>
        <label for="otp" class="block text-orange-400 font-medium mb-2">Enter OTP</label>
        <input type="text" id="otp" name="otp" required
          class="w-full px-4 py-2 border border-orange-600 bg-black/50 text-orange-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-transparent placeholder-orange-500">
      </div>

      <button type="submit"
        class="w-full bg-orange-600 hover:bg-orange-700 text-black font-bold py-2 rounded-lg transition duration-300 shadow-lg">
        Verify OTP
      </button>
    </form>

  
    <form action="/api/auth/resend-otp" method="POST" class="mt-4 text-center">
      <input type="hidden" name="email" id="resendEmail">
      <button type="submit" class="text-purple-400 hover:underline font-medium">Resend OTP</button>
    </form>

    <script>
     
      const emailInput = document.getElementById('email');
      const resendEmailInput = document.getElementById('resendEmail');
      emailInput.addEventListener('input', () => {
        resendEmailInput.value = emailInput.value;
      });
    </script>
  </div>
</body>
</html>
