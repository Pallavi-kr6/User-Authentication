<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-purple-600 to-blue-500 min-h-screen flex items-center justify-center">
  <div class="bg-white/90 backdrop-blur-md p-10 rounded-2xl shadow-2xl text-center max-w-md size-auto">
    <h2 class="text-3xl font-bold text-gray-800 mb-4">Welcome, <%= user.name %> 🎉</h2>
    <h3 class="text-2xl font-bold text-gray-800 size-auto"><%= user.email %></h3>
    <p class="text-gray-600 mb-6">You are now logged in.</p>
     <form action="/profile" >
      <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-6 rounded-lg transition duration-300">
        Edit Profile
      </button>
    </form>
    <br>
    <form action="/logout" method="POST">
      <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-6 rounded-lg transition duration-300">
        Logout
      </button>
    </form>
  </div>
</body>
</html>
